# Copy this file to .env.local and fill in your own values from the Upstash dashboard.
# Never commit .env.local to GitHub — it's already excluded in .gitignore.

UPSTASH_REDIS_REST_URL=https://your-database-name.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-rest-token-here
